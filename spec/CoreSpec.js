describe('flapjack', function() {

  var fixture = "";

  beforeEach(function() {
    setFixtures(fixture);
  });

  it('should exist', function() {
    expect(window.Flapjack).not.toBeUndefined();
  });

});
